export type AppConfig = {
    contacts: object;
    blogName: string,
    aboutPage: string,
    blogsFolder: string
};

export type BlogData = {
    blogTitle: string, 
    blogContent: string,
    blogLink: string,
    blogPreview: string
};
